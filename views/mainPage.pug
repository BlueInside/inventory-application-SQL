
extends layout

block content
    h2 Our games collection.

    
    if games 
        ul 
            each game in games
                li 
                    div 
                        img(src=game.image, alt=`${game.title} image`)
                        p= game.name 



    else 
        h3= `There's no games to display :(`