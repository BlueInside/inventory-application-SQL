
extends layout

block content
    h2 Browse our games collection.

    include searchForm.pug

    if games 
        ul 
            each game in games
                li 
                    a(href=`/game/${game.id}`)
                
                        div 
                            img(src=game.image, alt=`${game.title} image`)
                            p= game.title 



    else 
        h3= `There's no games to display :(`